version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            - "bash ./scripts/prebuild.sh"
        build:
          commands:
            - "echo Static site: no build step required"
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
